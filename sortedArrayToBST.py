# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def sortedArrayToBST(self, nums: List[int]) -> TreeNode:
        center = int(len(nums)/ 2)
        root = TreeNode()
        root.val = nums.pop(center)
        
        for i in range(center):
# left
                    
    
# right
            
        
        return root
        
        